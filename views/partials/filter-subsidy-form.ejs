<%
    const forms = [
    "Direct Grant",
    "Equity",
    "Guarantee",
    "Loan",
    "Purchase of goods services above market prices",
    "Provision of goods or services below market prices",
    "Tax measures (tax credit, or tax/duty exemption)",
    "Other"
    ];

    const subsidyForm = Array.isArray(filters.subsidyForm) ? filters.subsidyForm.map(l => l.toLowerCase()) : [(filters.subsidyForm || "").toLowerCase()];
    const allSelected = forms.every(form => subsidyForm.includes(form.toLowerCase()));
%>

<div class="govuk-form-group">
    <label class="govuk-label govuk-label--s" for="subsidyForm-checkbox">
        Subsidy form
    </label>

    <div class="govuk-checkboxes govuk-checkboxes--small filter-scroll-box" id="subsidyForm-checkbox">
        <div class="govuk-checkboxes__item">
            <input class="govuk-checkboxes__input js-select-all" id="formSelectAll" type="checkbox" <%= allSelected ? "checked" : "" %>>
            <label class="govuk-label govuk-checkboxes__label" for="formSelectAll">
                Select all
            </label>
        </div>

        <% forms.filter(f => f !== "Other").forEach((form, index) => { %>
        <div class="govuk-checkboxes__item">
            <input class="govuk-checkboxes__input form-checkbox" id="form-checkbox-<%=index%>" name="subsidyForm" type="checkbox" value="<%= form %>"
                <%= subsidyForm.includes(form.toLowerCase()) ? "checked" : "" %>
            >

            <label class="govuk-label govuk-checkboxes__label" for="form-checkbox-<%=index%>">
                <%= form %>
            </label>
        </div>
        <% }) %>

        <!-- Other -->
        <div class="govuk-checkboxes__item">
            <input class="govuk-checkboxes__input form-checkbox" id="form-checkbox-other" name="subsidyForm" type="checkbox" value="Other" data-other-checkbox
                <%= subsidyForm.includes("other") ? "checked" : "" %>
            >

            <label class="govuk-label govuk-checkboxes__label" for="form-checkbox-other">
                Other
            </label>
        </div>
        <div id="other-text-container" class="govuk-checkboxes__conditional" data-other-container>

            <label class="govuk-label" for="form-text-other">
                Please specify
            </label>

            <input class="govuk-input" id="form-text-other" name="subsidyFormOther" type="text"
                value="<%= filters.subsidyFormOther || '' %>"
            >
        </div>

    </div>
</div>